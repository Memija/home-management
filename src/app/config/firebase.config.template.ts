/**
 * Firebase configuration TEMPLATE
 *
 * SETUP INSTRUCTIONS:
 * 1. Copy this file and rename it to `firebase.config.ts` (same directory)
 * 2. Replace the placeholder values below with your actual Firebase project config
 * 3. You can find these values in the Firebase Console:
 *    - Go to https://console.firebase.google.com
 *    - Select your project
 *    - Click the gear icon → Project settings
 *    - Scroll down to "Your apps" → Web app config
 *
 * NOTE: firebase.config.ts is gitignored — your keys stay local.
 */
export const firebaseConfig = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'YOUR_PROJECT.firebaseapp.com',
  projectId: 'YOUR_PROJECT_ID',
  storageBucket: 'YOUR_PROJECT.appspot.com',
  messagingSenderId: 'YOUR_SENDER_ID',
  appId: 'YOUR_APP_ID'
};
